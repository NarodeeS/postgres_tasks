apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      volumes:
        - name: static-volume
          persistentVolumeClaim:
            claimName: static-volume
      restartPolicy: Always
      containers:
        - name: nginx
          image: narodees/postgres_tasks-nginx  # custom nginx image
          resources:
            limits:
              memory: "1Gi"
              cpu: "500m"
          ports:
            - containerPort: 80
          volumeMounts:
            - name: static-volume
              mountPath: /www/data/static
---
apiVersion: v1
kind: Service
metadata:
  name: nginx
spec:
  selector:
    app: nginx
  ports:
  - port: 80
    targetPort: 80
